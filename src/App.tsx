import {FC} from 'react';
import {Layout} from './components';
import './styles/index.scss';

const App: FC = () => {
    return (
        <div className="app">
            <Layout />
        </div>
    );
};

export default App;
